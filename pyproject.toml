[project]
name = "autoresearch-rs"
version = "0.1.0"
description = "Autonomous markdown prose research loop"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.6.0",
    "transformers>=4.49.0",
]
